/*
 * Copyright OpenSearch Contributors
 * SPDX-License-Identifier: Apache-2.0
 */

// Everything else (plugins, repositories, the common dependency set, test conventions) is inherited from
// the shared sandbox configuration in sandbox/build.gradle — a tenant's build.gradle looks just like this.
description = "Shared sandbox API (@ExperimentalAlgorithm) and the test-only fixture engine exercising " +
    "the k-NN engine extension points (KNNEngineDefinition registration, JNIService dispatch, query-param deferral)"
